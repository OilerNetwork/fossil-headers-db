# Production Environment Configuration for Fossil Headers DB Indexer

# Database Configuration
# RDS PostgreSQL connection string - replace with your actual RDS endpoint, credentials, and database
DB_CONNECTION_STRING=postgresql://username:password@your-rds-endpoint.region.rds.amazonaws.com:5432/fossil_headers_db

# Ethereum Node Configuration
# Production Ethereum mainnet RPC endpoint - replace with your preferred provider
NODE_CONNECTION_STRING=https://eth-mainnet.g.alchemy.com/v2/your-api-key

# HTTP Server Configuration
# Health check endpoint - typically bound to all interfaces in production
ROUTER_ENDPOINT=0.0.0.0:3000

# Indexing Configuration
# Index all blocks since genesis (block 0) - no START_BLOCK_OFFSET needed for full sync
# Set to true to index transaction data alongside block headers
INDEX_TRANSACTIONS=false

# Production Configuration
# Disable development mode for production
IS_DEV=false

# Logging Configuration
# Set to info for production to reduce log volume, or debug for detailed monitoring
RUST_LOG=info

# Additional Production Environment Variables (if needed)
# DB_MAX_CONNECTIONS=20
# DB_CONNECTION_TIMEOUT=30
